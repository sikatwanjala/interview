the npx create-react-app [app_name] to create the application.
cd app_name
npm install
npm start //to run the application.

the application will run on a single phase where all the operations are in a single page
